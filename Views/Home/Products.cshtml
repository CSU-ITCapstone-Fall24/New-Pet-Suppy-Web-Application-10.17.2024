@model IEnumerable<Pet_Web_Application_10._12._24_F.Models.Product>

<h2>Products</h2>
@foreach (var product in Model)
{
    <div>
        <h3>@product.Name</h3>
        <p>Price: $@product.Price</p>
        <a asp-action="AddToCart" asp-route-id="@product.Id">Add to Cart</a>
    </div>
}